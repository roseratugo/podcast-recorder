{
  "name": "@podcast-recorder/signaling",
  "version": "0.1.0",
  "private": true,
  "description": "WebRTC signaling server built with Rust",
  "scripts": {
    "dev": "cargo watch -x run",
    "build": "cargo build --release",
    "test": "cargo test",
    "lint": "cargo clippy -- -D warnings",
    "format": "cargo fmt",
    "clean": "cargo clean && rimraf node_modules"
  },
  "dependencies": {
    "@podcast-recorder/proto": "workspace:*"
  },
  "devDependencies": {}
}